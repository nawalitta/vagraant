{% extends "::base.html.twig" %}

{% block title %}RessourceBundle:Ressource:index{% endblock %}

{% block body %}
<h1>Welcome to the Ressource:index page</h1>
<nav class = "navbar navbar-default" >
    <div>
        <ul class = "nav navbar-nav">
            <li><a href = "{{path('RessourceBunde_Ressource_edit')}}">Creer une ressource</a></li>
        </ul>
     </div>
</nav>
        
<form>
    <div class="table-responsive">
        <table class="table">
             <caption>Liste des Ressources</caption>
            <tr>
                <th> # </th>
                <th> Nom </th>
                <th> Case à cocher </th>
            </tr>
        {% for ressource in ressources %}
            <tr>
                <td><a href={{path('RessourceBunde_Ressource_show',{ 'id': ressource.id })}}> {{ressource.id}}</a> </td>
                <td><a href={{path('RessourceBunde_Ressource_show',{ 'id': ressource.id })}}> {{ressource.designation}}</a> </td>
                <td> 
                    <div class="form-check">
                        <label class="form-check-label">
                            <input type="checkbox" name="idRessources[]" value="{{ressource.id}}" class="form-check-input">
                        </label>
                    </div>
                </td>
            </tr>
        {% endfor %}
        </table>
        <button type="submit" class="btn btn-primary">Supprimer</button>
</form>

{% endblock %}
